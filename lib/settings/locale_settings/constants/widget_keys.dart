import 'package:flutter/material.dart';

/// Value key of the draggable scrollable sheet of the locale settings options
/// modal bottom sheet.
const localeSettingsOptionsSheetKey = ValueKey('LocaleSettingsOptionsSheet');

/// Value key of the close button of the locale settings options modal bottom
/// sheet.
const localeSheetCloseButtonKey = ValueKey('LocaleSheetCloseButton');
